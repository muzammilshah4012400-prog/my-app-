I am trying to build an Android APK using GitHub Actions, but it keeps failing with 'Build FAILED' and 'exit code 1'. My project structure is:
[app, components, lib, public, android].
The 'android' folder contains 'gradlew', 'build.gradle', and 'settings.gradle'.
​Please act as a professional DevOps engineer. Analyze my current directory structure and provide a complete, error-free '.github/workflows/main.yml' file.
​IMPORTANT:
​Ensure the 'working-directory' is correctly set to 'android'.
​Include a step to 'sync' the Android project if needed (using 'npx cap sync android').
​Provide the exact path for 'upload-artifact' based on the standard Android build output.
​If any specific configuration in 'build.gradle' is needed to make this work on GitHub Actions, please tell me.
